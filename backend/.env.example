# Copie ce fichier en `.env` et adapte les valeurs.

# --- Mode dev par défaut : SQLite local (rien à faire, DATABASE_URL absent) ---

# --- Mode "copie de la BDD de prod" (MySQL/MariaDB local, via scripts/copy_prod_db.ps1) ---
# DATABASE_URL=mysql+pymysql://root:@127.0.0.1:3306/dru_mobile_copy

SECRET_KEY=change-me-in-prod
